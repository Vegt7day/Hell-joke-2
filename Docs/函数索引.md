# 脚本函数索引

> 涵盖项目内全部 58 个自有 GDScript 文件，按模块和 owner class 分类。
> 用于快速查询函数签名、所属类、访问级别和职责，供新增功能或修改现有功能时参考。
>
> **维护规则**：每次修改/新增/删除任何 .gd 文件中的函数、信号或枚举时，必须同步更新本文档对应条目。详见 `.cursor/rules/maintain-index.md`。
>
> 生成日期：2026-03-14

---

## 1. 全局单例 (Autoload)

### Game
**文件**: `system/globals/game/game.gd`
**extends**: `Node` | **Autoload**: `/root/Game`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `bind_player_stats_runtime(new_stats: Stats) -> void` | 运行时切换 Player Stats 数据源 |
| pub | `change_scene(path: String, params: Dictionary = {}) -> void` | 保存场景状态后切换场景，支持 entry_point/position/direction |
| pub | `has_save(slot: String = "savepoint") -> bool` | 检查指定存档槽位文件是否存在 |
| pub | `save_game(slot: String = "savepoint") -> void` | 序列化全部状态到 JSON 存档 |
| pub | `load_game(reset_current_scene: bool = false, slot: String = "savepoint") -> void` | 读档并恢复全部世界状态 |
| pub | `reload_scene_from_save(path: String, params: Dictionary = {}) -> void` | 读档专用场景重建 |
| pub | `new_game() -> void` | 清空所有会话状态，切换到初始世界场景 |
| pub | `on_world3_death_choice_load_heart() -> void` | "从心剑重来" 回调 |
| pub | `on_world3_death_choice_load_savepoint() -> void` | "从存档点重来" 回调 |
| priv | `_ready() -> void` | 初始化 color_rect 透明度和信号连接 |
| priv | `_is_autoload_game() -> bool` | 判断是否作为 Autoload 运行 |
| priv | `_wait_for_scene_load(tree: SceneTree, max_frames: int) -> void` | 等待场景加载（最多 N 帧） |
| priv | `_find_player_node_under_scene(scene: Node) -> Node` | BFS 查找玩家节点 |
| priv | `_save_path_for_slot(slot: String) -> String` | 存档路径映射 |
| priv | `_export_bucket_states(scene: Node) -> Dictionary` | 导出所有桶状态 |
| priv | `_apply_bucket_states(scene: Node, states: Dictionary) -> void` | 恢复桶状态 |
| priv | `_export_camera_cue_states(scene: Node) -> Dictionary` | 导出相机 Cue 状态 |
| priv | `_apply_camera_cue_states(scene: Node, states: Dictionary) -> void` | 恢复相机 Cue 状态 |
| priv | `_unhandled_input(event: InputEvent) -> void` | 全局输入：Esc 保存 / R 快速读档 |
| priv | `_is_world3_boss_scene(scene: Node) -> bool` | 判断是否为 World3 Boss 战斗场景 |
| priv | `_on_player_stats_changed() -> void` | 血量 <= 0 时弹出死亡重试 UI |
| priv | `_play_world3_death_fullscreen_flash() -> void` | 全屏白闪动画 |
| priv | `_open_world3_death_retry_ui() -> void` | 暂停游戏 + 白闪 + 实例化死亡 UI |

---

### MechanismLinkBus
**文件**: `system/globals/mechanism_link_bus.gd`
**extends**: `Node` | **Autoload**

**信号**: `channel_state_changed(channel_id: StringName, is_open: bool)` — 频道状态变更

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `publish_channel_state(channel_id: StringName, is_open: bool) -> void` | 发布频道状态并广播信号 |
| pub | `has_published_state(channel_id: StringName) -> bool` | 检查频道是否已有发布状态 |
| pub | `get_last_is_open(channel_id: StringName) -> bool` | 获取频道末次状态 |
| pub | `clear_last_states() -> void` | 清空所有频道记录（切场景用） |
| pub | `export_state() -> Dictionary` | 导出总线状态（存档用） |
| pub | `import_state(state: Dictionary, emit_signals: bool = false) -> void` | 导入总线状态（读档用） |
| pub | `rebroadcast_all_states() -> void` | 重新广播所有频道状态 |

---

### MechanismSfxBus
**文件**: `system/globals/mechanism_sfx_bus.gd`
**extends**: `Node` | **Autoload**

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `request_once(event_key: StringName, stream: AudioStream, cooldown_ms: int = -1) -> void` | 请求播放机关音效（去重 + 冷却节流） |
| priv | `_ready() -> void` | 确保音频总线树结构存在 |
| priv | `_play(stream: AudioStream) -> void` | 实际播放音效流 |
| priv | `_ensure_bus_tree() -> void` | 创建所有音频总线和 Send 路由 |
| priv | `_ensure_bus(bus_name: StringName) -> void` | 按名称确保总线存在 |
| priv | `_find_bus_index(bus_name: StringName) -> int` | 搜索总线索引，-1 表示未找到 |

---

### DialogicRegistry
**文件**: `system/dialogic/dialogic_registry.gd`
**extends**: `Node` | **Autoload**

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `register_character(character_id: String, node: Node, resource_path: String = "") -> bool` | 注册角色 |
| pub | `get_character_node(character_id: String) -> Node` | 按 ID 查找角色节点 |
| pub | `get_character_resource(character_id: String) -> String` | 按 ID 获取角色资源路径 |
| pub | `clear_all() -> void` | 清空所有注册 |
| pub | `save_registry() -> Dictionary` | 导出注册信息（存档用） |
| pub | `load_registry(data: Dictionary) -> void` | 恢复注册信息（读档用） |
| priv | `_ready()` | 打印就绪日志 + 预加载 .dch 角色资源 |\n| priv | `_preload_character_resources() -\u003e void` | 将 `character_resource_map` 中所有 .dch 加载到缓存 |

---

## 2. 实体系统 (Entities)

### Player
**文件**: `entities/player/real player/student.gd`
**class_name**: `Player` | **extends**: `CharacterBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `apply_camera_local_position_from_save(local_pos: Vector2) -> void` | 读档时直接写入 Camera2D.position |
| pub | `init_timers()` | 创建攻击/延迟/墨水恢复/召唤延迟四个 Timer |
| pub | `change_state(new_state: PlayerState)` | 状态机核心：退出旧状态 -> 进入新状态 |
| pub | `get_state_name(state: PlayerState) -> String` | 状态枚举转字符串 |
| pub | `enter_state(state: PlayerState)` | 进入状态：播放动画、触发音效、启动计时器 |
| pub | `exit_state(state: PlayerState)` | 退出状态：停止音效/计时器 |
| pub | `update_state(delta: float)` | 状态更新调度器 |
| pub | `handle_common_updates(delta: float)` | 通用更新：攻击/召唤冷却倒计时 |
| pub | `update_idle(delta: float)` | IDLE：移动/跳跃/攻击/召唤输入检查 |
| pub | `update_walk(delta: float)` | WALK：水平移动 + 朝向 + 状态转换 |
| pub | `update_jump_ascend(delta: float)` | JUMP_ASCEND：空中水平移动 + 重力 |
| pub | `update_jump_fall(delta: float)` | JUMP_FALL：空中水平移动 + 落地检测 |
| pub | `update_attack_start(delta: float)` | ATTACK_START：禁止移动，重力 |
| pub | `update_attack_shoot(delta: float)` | ATTACK_SHOOT：禁止移动，等待结束 |
| pub | `update_attack_end(delta: float)` | ATTACK_END：空等计时器 |
| pub | `update_stunned(delta: float)` | STUNNED：禁止移动，重力 |
| pub | `update_summon_start(delta: float)` | SUMMON_START：禁止移动，重力 |
| pub | `update_summon_end(delta: float)` | SUMMON_END：空等计时器 |
| pub | `can_jump() -> bool` | 检查是否可跳跃 |
| pub | `has_active_summoned_shangyang() -> bool` | 场景中是否有被召唤的商鞅 |
| pub | `can_attack_now() -> bool` | 输入启用 + 冷却归零 + 墨水够 |
| pub | `can_summon_shangyang_now() -> bool` | 输入启用 + 已解锁 + 冷却归零 + 无活跃商鞅 |
| pub | `start_attack()` | 切换到 ATTACK_START |
| pub | `unlock_shangyang_summon() -> void` | 解锁商鞅召唤能力 |
| pub | `try_summon_shangyang()` | 启动召唤冷却，切换到 SUMMON_START |
| pub | `get_summon_cooldown_ratio() -> float` | 返回召唤冷却进度 0~1 |
| pub | `execute_summon()` | 实例化商鞅并添加到场景 |
| pub | `calculate_summon_position() -> Vector2` | 根据朝向和偏移计算召唤位置 |
| pub | `return_to_normal_state()` | 根据地面/空中/速度回到 IDLE/WALK/JUMP |
| pub | `set_camera_focus_target_world(target_world: Vector2, move_speed: float = 220.0) -> void` | 设置相机焦点目标 |
| pub | `clear_camera_focus_target(move_speed: float = -1.0) -> void` | 退出焦点区，回正相机 |
| pub | `set_camera_drag_ignore_player_input(active: bool) -> void` | 拖拽时忽略玩家输入造成的相机偏移 |
| pub | `update_facing()` | 根据 direction 更新精灵翻转 |
| pub | `shoot()` | 实例化墨水子弹并发射 |
| pub | `calculate_shoot_position() -> Vector2` | 计算子弹发射位置 |
| pub | `apply_bump_from_horse(impulse: Vector2) -> bool` | 马身撞击击飞，带全局限流 |
| pub | `take_damage(damage_amount: float)` | 扣血（至少 1 点），播放受伤音效 |
| pub | `recover_full_health() -> void` | 瞬间回满血 |
| pub | `recover_full_health_once(interaction_id: String) -> bool` | 仅 interaction_id 不同时回满一次 |
| pub | `trigger_hit_shake_only() -> void` | 仅屏幕震动 |
| pub | `get_health()` | 返回 stats.health |
| pub | `get_direction() -> int` | 返回当前朝向 |
| pub | `set_direction(new_direction: int) -> void` | 设置朝向 |
| pub | `disable_action(action_name: String, ...)` | 禁用指定动作并可选设置回调 |
| pub | `enable_action(action_name: String)` | 启用指定动作 |
| pub | `is_action_disabled(action_name: String) -> bool` | 检查动作是否被禁用 |
| pub | `disable_all_actions()` | 禁用所有监控动作 |
| pub | `enable_all_actions()` | 启用所有监控动作 |
| pub | `register_interactable(v: Interactable)` | 注册可交互对象 |
| pub | `unregister_interactable(v: Interactable)` | 移除可交互对象 |
| pub | `register_contact_trigger(v: ContactTrigger) -> void` | 注册接触触发器 |
| pub | `unregister_contact_trigger(v: ContactTrigger) -> void` | 移除接触触发器 |
| priv | `_ready()` | 初始化 Stats、计时器、动画、物理检测 |
| priv | `_resolve_and_bind_stats_source() -> void` | 统一血量数据源 |
| priv | `_ensure_walk_sound_loop() -> void` | 确保走路音效循环播放 |
| priv | `_boss_fatal_summon_warning_active() -> bool` | Boss 濒死预警窗口激活？ |
| priv | `_notify_boss_warning_summon_ready(shangyang: ShangYang) -> void` | 通知 PhaseController 召唤就绪 |
| priv | `_physics_process(delta)` | 主循环：交互图标、输入、状态、移动、相机 |
| priv | `_resolve_player_camera2d() -> Camera2D` | 解析玩家 Camera2D |
| priv | `_update_camera_bias(delta: float) -> void` | 综合更新相机位置 |
| priv | `_update_lookahead(delta: float) -> void` | 相机前瞻偏移 |
| priv | `_get_move_input_axis() -> float` | 返回左右移动输入轴 |
| priv | `_on_attack_timer_timeout()` | 攻击计时器 -> ATTACK_SHOOT |
| priv | `_on_attack_delay_timer_timeout()` | 攻击延迟 -> ATTACK_END |
| priv | `_on_ink_recovery_timer_timeout()` | 墨水恢复（已停用连续恢复） |
| priv | `_on_summon_delay_timer_timeout()` | 召唤延迟 -> execute_summon |
| priv | `_on_animation_finished(anim_name: String)` | SUMMON draw 完成 -> SUMMON_END |
| priv | `_hurt_flash_modulate_toward_white(base: Color, blend: float) -> Color` | 向白色插值 |
| priv | `_kill_hit_shake_tween() -> void` | 停止屏幕震动 tween |
| priv | `_kill_sprite_hurt_flash_tween() -> void` | 停止精灵闪色 tween |
| priv | `_run_sprite_hurt_flash_pulse() -> void` | 精灵白色脉冲闪动 |
| priv | `_hit_shake_step(u: float) -> void` | 震动步骤 |
| priv | `_hit_shake_reset() -> void` | 重置震动 offset |
| priv | `_trigger_hit_screen_feedback() -> void` | 完整受击反馈 |
| priv | `_input(event)` | F1/J/F 调试快捷键 |
| priv | `_init_input_control()` | 初始化监控动作列表 |
| priv | `_process_controlled_input(event)` | 处理被禁用动作的输入 |

---

### Ink (子弹)
**文件**: `entities/player/bullet/ink.gd`
**extends**: `CharacterBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `initialize(start_position: Vector2, shoot_direction: bool)` | 设置起始位置和射击方向 |
| pub | `disable()` | 禁用子弹 |
| pub | `enable()` | 启用子弹 |
| priv | `_ready()` | 初始化方向、计时器、播放 shooting 动画 |
| priv | `_physics_process(delta)` | 移动子弹并检测碰撞 |
| priv | `_handle_collision(collision: KinematicCollision2D)` | 碰撞处理：停速 + 消失动画 + 传递伤害 |
| priv | `_on_lifetime_timer_timeout()` | 存活到期 -> 消失动画 -> 销毁 |
| priv | `_destroy_bullet()` | 停止动画并 queue_free |

---

### Teacher (老教师)
**文件**: `entities/enemy/teacher/teacher.gd`
**extends**: `CharacterBody2D`

**信号**: `teacher_time_out` / `health_depleted` / `student_intro_triggered`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `create_default_spawn_point()` | 创建默认发射点 Marker2D |
| pub | `start_attacking(interval: float = 2.0)` | 开始攻击循环 |
| pub | `stop_attacking()` | 停止攻击 |
| pub | `set_shoot_interval(interval: float)` | 修改射击间隔 |
| pub | `start_auto_damage()` | 开始每秒自动扣血 |
| pub | `stop_auto_damage()` | 停止自动扣血 |
| pub | `shoot()` | 发射子弹（动画 + 延迟 + 实例化） |
| pub | `take_damage(damage_amount: float)` | 受击入口 |
| pub | `apply_damage(damage_amount: float)` | 实际扣血+归零检查 |
| pub | `apply_auto_damage(damage_amount: float)` | 自动扣血+归零检查 |
| pub | `check_health_zero()` | 归零时发信号、隐藏血条 |
| pub | `die()` | 死亡：停止攻击 + 延迟销毁 |
| priv | `_ready()` | 初始化发射点、计时器、Stats、血条 |
| priv | `_process(delta)` | 自动扣血循环 |
| priv | `_physics_process(delta)` | 死亡时跳过物理 |
| priv | `_on_shoot_timer_timeout()` | 射击计时器 -> shoot() |
| priv | `_on_student_intro_triggered()` | 对话触发后：隐藏血条 |

---

### 蚊子 (敌人子弹)
**文件**: `entities/enemy/bullet/蚊子.gd`
**extends**: `CharacterBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `initialize(spawn_position: Vector2, shoot_direction: bool, bullet_speed: float = 300.0, bullet_damage: float = 0.05)` | 保存并应用初始化参数 |
| pub | `apply_initialization()` | 应用已保存的初始化参数 |
| pub | `handle_collision(collision: KinematicCollision2D)` | 碰撞后附着到目标或销毁 |
| pub | `stick_to_collider(target: Node2D)` | 附着到碰撞体 |
| pub | `destroy()` | 停止计时器并 queue_free |
| priv | `_ready()` | 初始化方向、计时器、动画和音效 |
| priv | `_physics_process(delta)` | stick 跟随 + 持续伤害；collided 则停止 |
| priv | `_on_stick_timer_timeout()` | 附着到期 -> 销毁 |
| priv | `_on_lifetime_timer_timeout()` | 存活到期 -> 销毁或启附着计时器 |
| priv | `_enter_tree()` | 若有待初始化参数则应用 |

---

### ShangYang (商鞅 NPC)
**文件**: `entities/npc/商鞅/商鞅.gd`
**class_name**: `ShangYang` | **extends**: `CharacterBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `setup_summoned_for_boss_fatal_warning() -> void` | Boss 预警期间召唤：不可碰撞/免疫伤害 |
| pub | `switch_to_summoned_mode()` | 切换到被召唤模式 |
| pub | `switch_to_story_mode()` | 切换到剧情模式 |
| pub | `can_accept_limb_pickup() -> bool` | 能否接受肢体拾取 |
| pub | `can_use_story_interact() -> bool` | 能否使用剧情交互 |
| pub | `is_awaiting_story_interact() -> bool` | 是否等待玩家交互 |
| pub | `restore_post_get3_waiting_interact() -> void` | 恢复"获得能力"等待状态（读档） |
| pub | `notify_limb_collected() -> void` | 肢体收集通知 -> 播放 get 动画 |
| pub | `consume_pending_return_sy_dialog() -> void` | 消耗"回去看商鞅"标记并启动对话 |
| pub | `on_player_story_interact() -> void` | 玩家交互回调 -> 启动"获得商鞅能力"对话 |
| pub | `play_get_animation(animation_index: int = 0)` | 播放获取形态动画 |
| pub | `play_next_get_animation()` | 按顺序播放下一个获取动画 |
| pub | `take_damage(damage_amount: float, attacker: Node2D = null)` | 受击：击退 + 扣血 |
| pub | `cancel_summoned_lifetime_limit() -> void` | 取消召唤存活限制 |
| pub | `force_initial_story_form() -> void` | Boss 演出强制显示 ready_to_pull 态 |
| pub | `play_be_pull_animation() -> void` | 播放被拉扯动画 + 音效 |
| pub | `enter_post_boss_pull_story_state() -> void` | Boss 分尸后：幽灵态 |
| pub | `get_limb_marker_nodes() -> Array[Node2D]` | 返回所有 LimbMarkers 子节点 |
| pub | `apply_story_progress_without_cutscene(saved_pickup_count: int) -> void` | 读档快照到指定形态 |
| pub | `export_story_progress_state() -> Dictionary` | 导出剧情进度（存档用） |
| pub | `apply_story_progress_state(state: Dictionary) -> void` | 应用剧情进度（读档用） |
| pub | `get_status() -> Dictionary` | 返回当前状态信息字典 |
| pub | `safe_destroy()` | 安全销毁 |
| priv | `_ready()` | 连接动画信号，按模式初始化 |
| priv | `_initialize_story_mode()` | 剧情模式初始化 |
| priv | `_initialize_summoned_mode()` | 召唤模式初始化 |
| priv | `_deferred_limb_pickup_cutscene(animation_index: int) -> void` | 延迟执行肢体拾取 cutscene |
| priv | `_run_get_cutscene(animation_index: int) -> void` | 运行获取 cutscene |
| priv | `_start_return_to_sy_dialog() -> void` | 启动"回去看商鞅"对话 |
| priv | `_start_world2_ability_dialog() -> void` | 启动"获得商鞅能力"对话 |
| priv | `_grant_player_summon_unlock() -> void` | 解锁玩家召唤能力 |
| priv | `_run_disable_destroy_sequence() -> void` | 播放 disable 动画 -> 淡出 -> 销毁 |
| priv | `_check_health_thresholds()` | 检查 75/50/25/0% 血量阈值 |
| priv | `_play_damage_animation(threshold_index: int)` | 播放伤害动画 |
| priv | `_on_animation_finished(anim_name: String)` | 动画完成状态转换 |
| priv | `_start_summoned_lifetime_countdown() -> void` | 启动召唤存活倒计时 |
| priv | `_run_summoned_lifetime_countdown(token: int) -> void` | 倒计时到期 -> 消失动画 -> 销毁 |
| priv | `_physics_process(delta: float) -> void` | 重力 + 击退衰减 + 玩家推动 |
| priv | `_apply_summoned_damage(damage_amount: float) -> void` | 扣血 + 标记死亡 |
| priv | `_apply_hit_knockback(attacker: Node2D = null) -> void` | 击退冲量 |
| priv | `_apply_player_push_resistance() -> void` | 玩家推动阻力 |
| priv | `_snap_get_animation_to_end(animation_index: int) -> void` | 跳到获取动画最后一帧 |

---

### ShangYangLimb (商鞅肢体)
**文件**: `entities/npc/商鞅/shang_yang_limb.gd`
**class_name**: `ShangYangLimb` | **extends**: `Interactable`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `set_limb_pickups_enabled(enabled: bool) -> void` | 启用/禁用肢体拾取 |
| pub | `apply_saved_collected() -> void` | 标记已收集并销毁（读档用） |
| pub | `interact() -> void` | 播放 disappear 动画 -> 通知商鞅 -> 销毁 |
| priv | `_ready() -> void` | 播放 idle 动画，加入 shangyang_limb 组 |
| priv | `_overlapping_player() -> Player` | 返回重叠的 Player 节点 |

---

### ShangYangStoryInteract (商鞅剧情交互)
**文件**: `entities/npc/商鞅/shang_yang_story_interact.gd`
**class_name**: `ShangYangStoryInteract` | **extends**: `Interactable`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `set_interact_enabled(on: bool) -> void` | 启用/禁用交互监测 |
| pub | `interact() -> void` | 调用父级商鞅 `on_player_story_interact()` |

---

## 3. 关卡系统 (Levels)

### WorldController
**文件**: `system/levels/world.gd`
**class_name**: `WorldController` | **extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `mark_dialog_timeline_completed(timeline_id: String) -> void` | 标记对话时间线已完成 |
| pub | `has_completed_timeline(timeline_id: String) -> bool` | 检查时间线是否已完成 |
| pub | `register_characters_globally() -> void` | 注册"中学生"和"老教师"到 DialogicRegistry |
| pub | `test_registry(registry) -> void` | 测试注册结果 |
| pub | `receive(sig: String)` | 接收 Dialogic 信号事件 |
| pub | `start_dialogue()` | 启动教师介绍对话时间线 |
| pub | `get_teacher_dialog_marker() -> Marker2D` | 查找教师节点下的对话 Marker |
| pub | `get_student_dialog_marker() -> Marker2D` | 查找玩家节点下的对话 Marker |
| pub | `start_level()` | 启动关卡：激活状态、开始攻击、计时器 |
| pub | `start_teacher_attack()` | 触发教师自动扣血攻击 |
| pub | `check_player_health()` | 检查玩家血量，<=0 触发死亡 |
| pub | `on_player_death()` | 处理玩家死亡：标记失败、结局对话 |
| pub | `play_ending_dialogue(is_victory: bool)` | 播放学生逃跑结局对话 |
| pub | `end_level()` | 结束关卡 |
| pub | `show_level_result()` | 显示胜利/失败/逃跑结果 |
| pub | `show_message(message: String, duration: float = 3.0)` | 显示 UI 消息 |
| pub | `to_dict() -> Dictionary` | 导出存档数据 |
| pub | `from_dict(dict: Dictionary) -> void` | 从存档恢复状态 |
| pub | `update_player(position: Vector2, direction: int = 1) -> void` | 恢复玩家位置和朝向 |
| priv | `_ready() -> void` | 初始化计时器、连接门信号、启动对话 |
| priv | `_physics_process(delta)` | 关卡活跃时累积时间 + 检查血量 |
| priv | `_apply_post_intro_dialogue_state() -> void` | 对话已结束则直接进入关卡 |
| priv | `_on_intro_dialogue_ended()` | 教师介绍对话结束回调 |
| priv | `_on_teacher_time_out()` | 教师时间结束：玩家胜利 |
| priv | `_on_level_timer_timeout()` | 关卡计时器超时 |
| priv | `_on_door_area_entered(body: Node2D)` | 玩家进入门区域 |
| priv | `_on_ending_dialogue_ended()` | 结局对话结束回调 |

---

### World2
**文件**: `system/levels/world2/world_2.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `mark_dialog_timeline_completed(timeline_id: String) -> void` | 标记对话时间线已完成 |
| pub | `has_completed_timeline(timeline_id: String) -> bool` | 检查时间线是否已完成 |
| pub | `run_shangyang_get_cutscene(sy: ShangYang, anim_index: int) -> void` | 播放商鞅获取肢体过场动画 |
| pub | `mark_shangyang_npc_removed() -> void` | 标记商鞅 NPC 已被移除 |
| pub | `register_shangyang_limb_collected(limb_name: StringName) -> void` | 记录已收集的商鞅肢体名称 |
| pub | `to_dict() -> Dictionary` | 导出存档数据 |
| pub | `from_dict(dict: Dictionary) -> void` | 从存档恢复全部状态 |
| pub | `update_player(position: Vector2, direction: int = 1) -> void` | 恢复玩家位置和朝向 |
| priv | `_ready() -> void` | 清空 MechanismLinkBus 状态，延迟初始化 world2 |
| priv | `_world2_quest_setup() -> void` | 恢复存档肢体、注册角色、启动 intro 对话 |
| priv | `_register_dialogic_characters() -> void` | 将"中学生"和"商鞅"注册到 DialogicRegistry |
| priv | `_restore_limbs_from_save() -> void` | 从存档恢复肢体状态 |
| priv | `_start_intro_dialog() -> void` | 启动商鞅开场对话 |
| priv | `_on_intro_dialog_finished(_arg = null) -> void` | 对话完成 -> 启用肢体拾取 |
| priv | `_infer_cutscene_completions_from_saved_limbs() -> void` | 根据已收集肢体推断过场完成 |

---

### World3BossArena
**文件**: `system/levels/world3/world3_boss_arena.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `to_dict() -> Dictionary` | 导出完整 Boss 竞技场存档状态 |
| pub | `from_dict(dict: Dictionary) -> void` | 从存档恢复全部状态 |
| pub | `update_player(position: Vector2, direction: int = 1) -> void` | 恢复玩家位置和朝向 |
| pub | `set_pending_shared_hp_override_for_save(value: int) -> void` | 设置存档时覆盖的共享 HP 值 |
| pub | `request_delayed_heart_save(predicted_hp: int, target_minor_name: StringName) -> void` | 请求延迟心剑存档 |
| pub | `consume_no_damage_heart_interaction_token() -> bool` | 消耗一次无伤心剑交互次数 |
| priv | `_ready() -> void` | 初始化摔死 Y、心剑交互物、Boss 音频 |
| priv | `_setup_boss_audio() -> void` | 连接 Boss 阶段/血量信号到 BGM 切换 |
| priv | `_boss_bgm_crossfade_to(target: _ArenaBgmSlot, duration: float) -> void` | BGM 交叉渐变 |
| priv | `_on_boss_phase_changed(new_phase: Variant) -> void` | 阶段变化 -> Stinger + BGM 切换 |
| priv | `_on_shared_health_depleted_bgm() -> void` | Boss 血量归零 -> 切回普通 BGM |
| priv | `_physics_process(_delta: float) -> void` | 检测玩家是否掉出边界摔死 |
| priv | `_process(delta: float) -> void` | 处理延迟心剑存档逻辑 |
| priv | `_capture_node_state(node: Node2D) -> Dictionary` | 捕获节点状态 |
| priv | `_apply_node_state(node: Node2D, state: Dictionary) -> void` | 应用存档状态 |

---

## 4. Boss 系统 (World3)

### BossHorseMain (主马)
**文件**: `system/levels/world3/bosses/boss_horse_main.gd`
**extends**: `CharacterBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `take_damage(damage_amount: float, _attacker: Variant = null) -> void` | 受击委托 PhaseController 结算共享伤害 |
| pub | `set_movement_enabled(enabled: bool) -> void` | 启用/禁用移动 |
| pub | `post_skill_broadcast(skill: int, is_main_horse: bool) -> void` | 发送技能广播弹幕 |
| pub | `post_minor_entry_broadcast(phase_raw: int) -> void` | 发送小马进入直播间广播 |
| priv | `_ready() -> void` | 初始化：分组、PhaseController、血条、碰撞区 |
| priv | `_process(delta: float) -> void` | 技能循环：冷却管理 + 随机施放 |
| priv | `_physics_process(delta: float) -> void` | 移动：离屏传送 + PID 追踪 + move_and_slide |
| priv | `_resolve_phase_controller() -> void` | 查找 PhaseController |
| priv | `_try_cast_random_skill() -> void` | 随机选择已解锁技能并施放 |
| priv | `_cast_grey_skill() -> void` | 灰马技能：爱的冲撞（向左冲刺） |
| priv | `_cast_white_skill() -> void` | 白马技能：认真一剑（召唤剑 x2 波） |
| priv | `_cast_black_skill() -> void` | 黑马技能：诡影兵团（召唤分身 + 同步动画 + 技能循环） |
| priv | `_cast_red_skill() -> void` | 红马技能：猩红风暴（召唤炸弹） |
| priv | `_restore_idle_jump() -> void` | 恢复默认 jump 待机动画 |
| priv | `_play_optional(anim_name: StringName, fallback_seconds: float) -> void` | 播放可选动画 |
| priv | `_toggle_root_collision_shape_disabled(disabled: bool) -> void` | 切换根碰撞体禁用状态 |
| priv | `_move_to_position(target: Vector2, duration: float) -> void` | Tween 移动主马 |
| priv | `_camera_viewport_world_x_range() -> Vector2` | 计算摄像机视口世界 X 范围 |
| priv | `_is_in_skill_cast_horizontal_band(subject: Node2D) -> bool` | 检查角色是否在技能施放水平带内 |
| priv | `_is_outside_camera_bounds() -> bool` | 检查主马是否在可视区外 |
| priv | `_respawn_to_camera_right_quarter_down() -> void` | 传送主马到屏幕右侧外 |
| priv | `_compute_pid_tracking_velocity(delta: float) -> Vector2` | PID 追踪速度（X 向左，Y 延迟跟随玩家） |
| priv | `_get_view_bounds_x() -> Vector2` | 获取视口水平边界 |
| priv | `_spawn_white_swords(spawn_count: int) -> void` | 生成白马飞剑 |
| priv | `_spawn_red_bombs(spawn_count: int) -> void` | 生成红马炸弹 |
| priv | `_spawn_black_clone() -> CharacterBody2D` | 生成黑马分身实例 |
| priv | `_run_black_clone_skill_loop(clone: CharacterBody2D) -> void` | 驱动分身技能循环 |
| priv | `_pick_clone_skill() -> BossHorseTypes.HorseId` | 为分身随机选择技能 |
| priv | `_cast_clone_skill(clone, skill) -> void` | 分身施放指定技能 |
| priv | `_fade_and_free_clone(clone: CharacterBody2D) -> void` | 渐隐并销毁分身 |
| priv | `_skill_display_name(skill: BossHorseTypes.HorseId) -> String` | 技能 ID 转中文名 |
| priv | `_horse_display_name(skill, is_main_horse: bool) -> String` | 马色 ID 转中文名 |
| priv | `_show_skill_broadcast(skill, is_main_horse: bool) -> void` | 显示技能弹幕广播 |
| priv | `_resolve_skill_feed_root() -> Control` | 解析弹幕根节点 |
| priv | `_create_skill_feed_row(feed_root: Control, message_bbcode: String, is_entry: bool = false) -> Control` | 创建弹幕行控件 |
| priv | `_push_skill_feed_message(feed_root: Control, message_bbcode: String, is_entry: bool = false) -> void` | 推送弹幕消息并启动滚动动画 |
| priv | `_pick_danmaku_lane(feed_root: Control) -> int` | 选取空闲弹幕轨道 |
| priv | `_is_danmaku_fully_inside_right_boundary(container: Control, lane_right_x: float) -> bool` | 检查弹幕是否完全在右边界内 |

---

### BossHorseMinor (小马)
**文件**: `system/levels/world3/bosses/boss_horse_minor.gd`
**extends**: `CharacterBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `take_damage(damage_amount: float, _attacker: Variant = null) -> void` | 受击委托 PhaseController 结算共享伤害 |
| pub | `set_movement_enabled(enabled: bool) -> void` | 启用/禁用移动并清零速度 |
| pub | `refresh_visual_to_horse_id() -> void` | 根据 horse_id 强制对齐纹理和区域 |
| priv | `_ready() -> void` | 复制 AtlasTexture（防共享污染）、分组、刷新视觉 |
| priv | `_physics_process(delta: float) -> void` | 移动：分身跟随/普通左移 + 离屏传送 |
| priv | `_process(delta: float) -> void` | 技能循环：召唤分身/自动施放技能 |
| priv | `_resolve_phase_controller() -> void` | 查找 PhaseController |
| priv | `_camera_viewport_world_x_range() -> Vector2` | 摄像机视口世界 X 范围 |
| priv | `_is_in_skill_cast_horizontal_band() -> bool` | 检查是否在技能施放水平带内 |
| priv | `_is_outside_camera_bounds() -> bool` | 检查是否在可视区外 |
| priv | `_respawn_to_camera_right_at_player_y() -> void` | 传送小马到屏幕右侧外对齐玩家 Y |
| priv | `_get_view_bounds_x() -> Vector2` | 获取视口水平边界 |
| priv | `_cast_grey_dash_skill() -> void` | 灰马冲刺 |
| priv | `_cast_white_summon_skill() -> void` | 白马召唤剑 |
| priv | `_cast_red_bomb_skill() -> void` | 红马召唤炸弹 |
| priv | `_move_to_position(target: Vector2, duration: float) -> void` | Tween 移动 |
| priv | `_play_optional(anim_name: StringName, fallback_seconds: float) -> void` | 播放可选动画（含纹理污染修复） |
| priv | `_spawn_white_sword() -> void` | 生成白马飞剑 |
| priv | `_spawn_black_minor_clone() -> void` | 黑马召唤无技能跟随分身 |
| priv | `_begin_summoned_clone_lifetime(duration: float) -> void` | 分身生命周期管理 |
| priv | `_spawn_red_bomb() -> void` | 生成红马炸弹 |

---

### BossSi (驷马)
**文件**: `system/levels/world3/bosses/boss_si.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `take_damage(damage_amount: float, _attacker: Variant = null) -> void` | 受击委托 PhaseController |
| pub | `set_movement_enabled(enabled: bool) -> void` | 启用/禁用移动 |
| priv | `_ready() -> void` | 驷马初始化：分组、PhaseController、碰撞区 |
| priv | `_physics_process(delta: float) -> void` | 左移 + 离屏回绕 |
| priv | `_resolve_phase_controller() -> void` | 查找 PhaseController |
| priv | `_ensure_player_bump_area() -> void` | 创建玩家撞击区域 |
| priv | `_wrap_to_right_if_out_of_view() -> void` | 超出左侧 -> 回绕到右侧 |

---

### BossHorsePlayerBumpArea
**文件**: `system/levels/world3/bosses/boss_horse_player_bump_area.gd`
**class_name**: `BossHorsePlayerBumpArea` | **extends**: `Area2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `setup_shape_from_horse(h: CharacterBody2D, y_scale: float = 0.82) -> void` | 从父马复制碰撞几何 |
| pub | `set_bump_enabled(on: bool) -> void` | set_deferred 控制 monitoring |
| pub | `apply_standard_bump_preset() -> void` | 应用标准击飞参数 |
| priv | `_ready() -> void` | 初始化碰撞层/掩码、连接信号 |
| priv | `_on_body_entered(body: Node2D) -> void` | 玩家进入 -> 击飞冲量 |
| priv | `_on_body_exited(body: Node2D) -> void` | 玩家离开 -> 重新武装 |

---

### BossHorsePhaseController
**文件**: `system/levels/world3/scripts/boss_horse_phase_controller.gd`
**extends**: `Node`

**信号**: `phase_changed(new_phase)` / `shared_health_changed(current, max, percent)` / `shared_health_depleted` / `intro_timeline_finished` / `boss_combat_bgm_requested`

**枚举**: `BossPhase` — INTRO, SI_TO_FOUR_VISUAL, FOUR_EXIT_GREY_ENTER, GREY_SOLO, WHITE_SOLO, BLACK_SOLO, RED_SOLO, FINAL_WARNING_20, CHAIN_CINEMATIC, ALL_HORSES

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `is_summon_during_fatal_warning_window() -> bool` | 是否在致命预警窗口内 |
| pub | `is_fatal_pull_sequence_active() -> bool` | 是否处于 20% 致命拉扯演出中 |
| pub | `on_warning_summoned_shangyang_ready(shangyang: Node2D) -> void` | 预警期间商鞅就绪 -> 预生成肢体 |
| pub | `request_phase(next: BossHorseTypes.BossPhase) -> void` | 请求切换 Boss 阶段 |
| pub | `apply_shared_damage(damage_amount: float, source_name: String = "unknown") -> void` | 应用共享血量伤害 |
| pub | `trigger_external_percent_damage(percent: float = 0.2, source_name: String = "interaction_target") -> void` | 外部百分比扣血（心剑交互） |
| pub | `get_shared_stats() -> Stats` | 返回共享 Stats 引用 |
| pub | `mark_intro_battle_triggered_for_save() -> void` | 标记 intro 战斗已触发 |
| pub | `is_intro_battle_triggered_for_save() -> bool` | 检查 intro 战斗是否已触发 |
| pub | `is_si_split_triggered_for_save() -> bool` | 检查驷马分裂是否完成 |
| pub | `apply_intro_battle_triggered_from_save(triggered: bool) -> void` | 从存档应用 intro 战斗触发状态 |
| pub | `force_main_si_enter_battle_from_save() -> void` | 读档强制主马 + 驷马进入战斗态 |
| pub | `ensure_saved_minors_present(minors_state: Dictionary) -> void` | 读档时确保存档中的小马节点存在 |
| pub | `restore_minor_horse_ids(minors_root: Node) -> void` | 恢复小马节点的 horse_id |
| pub | `restore_combat_state_from_save(saved_phase: int, should_restore: bool) -> void` | 从存档恢复战斗状态 |
| priv | `_ready() -> void` | 初始化：分组、清空 Minors、连接血量信号、intro 时间线 |
| priv | `_on_main_health_changed() -> void` | 共享血量变化 -> 更新阶段、触发致命演出 |
| priv | `_update_phase_by_hp_percent(hp_percent: float) -> void` | 根据血量百分比推进 Boss 阶段 |
| priv | `_get_hp_percent() -> float` | 计算当前血量百分比 |
| priv | `_run_boss_intro_timeline() -> void` | Boss 入场完整时间线 |
| priv | `_run_minor_switch(target_phase: BossHorseTypes.BossPhase) -> void` | 小马切换（离场 + 入场） |
| priv | `_apply_cumulative_minors_for_phase(phase: BossPhase) -> void` | 为阶段激活所有应有小马 |
| priv | `_get_or_spawn_minor(minors: Node2D, minor_name: StringName) -> Node2D` | 获取或动态生成小马节点 |
| priv | `_fatal_attack_full_sequence() -> void` | 致命攻击完整序列 |
| priv | `_run_shangyang_limb_pull_sequence_for(shangyang: Node2D, keep_input_lock: bool) -> void` | 商鞅五马分尸牵引序列 |
| priv | `_run_player_fatal_ropes_pull() -> void` | 无商鞅时玩家五绳拉扯序列 |
| priv | `_play_relimb_timeline_deferred() -> void` | 重拾肢体时间线 |
| priv | `_after_sy_pull_start_finale_siege() -> void` | 分尸对话后五马回场合围 |
| priv | `_scatter_horses_flee_async() -> void` | 五马四散逃离动画 |
| priv | `_spawn_rope_between(parent: Node2D, horse: Node2D, limb: Node2D) -> Node2D` | 生成绳子 |
| priv | `_tween_fatal_pull_horses_surround(target: Vector2, horses: Array) -> void` | Tween 五马围到目标周围 |
| priv | `_tween_finale_horses_enter_scene(horses: Array, main, minors, shang_center) -> void` | Tween 终局马匹入场 |
| priv | `_move_pair(first, first_target, second, second_target, duration: float) -> void` | Tween 同步移动两个节点 |
| priv | `_move_pair_by_speed(first, first_target, second, second_target, speed: float) -> void` | 按速度同步移动 |
| priv | `_target_outside_camera(node: Node2D, direction: Vector2, margin: float) -> Vector2` | 计算移出视野外的目标位置 |
| priv | `_set_horse_movement(node: Node, enabled: bool) -> void` | 调用马的 set_movement_enabled |
| priv | `_preload_all_dialogic_timelines() -> void` | 预加载全部 Dialogic 时间线，避免首次同步解析卡顿 |
| priv | `_await_dialog_timeline(timeline_id: String, fallback_res: String) -> void` | 等待 Dialogic 时间线结束 |
| priv | `_await_seconds_safe(seconds: float) -> void` | 安全等待 |
| priv | `_await_process_frame_safe() -> bool` | 安全等待一帧 |

---

### BossHorseTypes
**文件**: `system/levels/world3/scripts/boss_horse_types.gd`
**class_name**: `BossHorseTypes` | **extends**: `RefCounted`

**枚举**: `HorseId` — GREY=0, WHITE=1, BLACK=2, RED=3

| 访问 | 签名 | 描述 |
|------|------|------|
| pub_static | `phase_to_text(phase: BossPhase) -> String` | 阶段枚举转字符串 |

---

## 5. World3 道具/辅助

### World3RandomPieceBuilder
**文件**: `system/levels/world3/scripts/world3_random_piece_builder.gd`
**class_name**: `World3RandomPieceBuilder` | **extends**: `Node`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `get_selected_pool_indices() -> Array[int]` | 返回选中的模块池索引列表 |
| pub | `get_generation_seed() -> int` | 返回当前生成种子 |
| pub | `apply_generation_from_indices(indices: Array, seed_opt: int = 0) -> void` | 从索引序列重建模块拼接（存档用） |
| pub | `ensure_neighbors_loaded(center_index: int) -> void` | 确保中心模块前后各一模块已加载 |
| pub | `trim_to_neighbors(center_index: int) -> void` | 裁剪超出邻域的模块 |
| pub | `mark_heart_used_on_piece_index(index: int) -> void` | 标记指定模块的心剑已被使用 |
| pub | `export_builder_state() -> Dictionary` | 导出构造器存档状态 |
| pub | `apply_builder_state(state: Dictionary) -> void` | 恢复构造器状态 |
| priv | `_ready() -> void` | 初始化、验证标记点、执行随机拼接 |
| priv | `_build_sequence_with_seed(start_marker: Marker2D, runtime_root: Node2D) -> void` | 种子随机拼接模块序列 |
| priv | `_select_piece_indices(count: int) -> void` | 随机选取模块池索引 |
| priv | `_spawn_piece_if_needed(index: int, runtime_root: Node2D) -> void` | 实例化模块 |
| priv | `_bind_piece_streaming_areas(piece: Node2D, index: int) -> void` | 绑定流送区信号 |
| priv | `_on_outer_streaming_body_entered(body: Node, index: int) -> void` | 加载邻域模块 |
| priv | `_on_inner_streaming_body_entered(body: Node, index: int) -> void` | 裁剪远端模块 |
| priv | `_capture_piece_runtime_state(index: int, piece: Node2D) -> void` | 捕获模块运行时状态 |
| priv | `_apply_cached_piece_runtime_state_if_any(index: int, piece: Node2D) -> void` | 应用缓存的模块状态 |
| priv | `_should_spawn_heart_for_index(index: int) -> bool` | 判断是否应生成心剑交互物 |
| priv | `_attach_heart_interactable_if_needed(index: int, piece: Node2D) -> void` | 生成心剑交互物 |

---

### ShangyangDialogProxy
**文件**: `system/levels/world3/scripts/shangyang_dialog_proxy.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `set_tracking(enabled: bool) -> void` | 启用/禁用每帧跟随商鞅位置 |
| pub | `sync_now() -> void` | 立即同步位置 |
| pub | `get_dialog_anchor() -> Node2D` | 返回对话锚点 |
| priv | `_process(_delta: float) -> void` | 追踪模式下每帧同步位置 |
| priv | `_resolve_shangyang_visual() -> Node2D` | 解析商鞅视觉节点 |

---

### BossDamageInteractable (心剑交互)
**文件**: `system/levels/world3/props/boss_damage_interactable.gd`
**class_name**: `BossDamageInteractable` | **extends**: `Interactable`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `interact() -> void` | 交互：震动 -> 百分比扣血 -> 存档 -> 播放 hurt 动画 |
| pub | `export_save_state() -> Dictionary` | 导出租档状态 |
| pub | `apply_save_state(state: Dictionary) -> void` | 应用存档状态 |
| priv | `_ready() -> void` | 开启监测、播放 idle 动画 |
| priv | `_resolve_phase_controller() -> Node` | 解析 PhaseController |
| priv | `_overlapping_player() -> Player` | 获取重叠的玩家 |
| priv | `_play_if_exists(anim_name: StringName) -> void` | 播放可选动画 |
| priv | `_resolve_newly_unlocked_minor_name(cur, predicted) -> StringName` | 根据血量跨越阈值判断新解锁小马名 |

---

### ShangYangLimbPiece (商鞅肢体碎片)
**文件**: `system/levels/world3/props/shangyang_limb_piece.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `apply_limb_visual(kind: LimbType) -> void` | 按类型设置纹理 |
| pub | `set_follow_target(target: Node2D) -> void` | 设置跟随目标 |
| pub | `freeze_follow() -> void` | 冻结跟随 |
| priv | `_ready() -> void` | 应用肢体视觉 |
| priv | `_pick_texture_by_type(kind: LimbType) -> Texture2D` | 按类型返回贴图 |
| priv | `_process(_delta: float) -> void` | 跟随模式下每帧更新位置 |

---

### SavePointInteractable
**文件**: `system/levels/world3/props/save_point_interactable.gd`
**class_name**: `SavePointInteractable` | **extends**: `Interactable`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `interact() -> void` | 震动 -> 回满血 -> 存档 -> 播放动画序列 |
| priv | `_ready() -> void` | 开启监测、停止动画 |
| priv | `_play_if_exists(anim_name: StringName) -> void` | 播放可选动画 |

---

### ProjectileWhiteHorse (白马飞剑)
**文件**: `system/levels/world3/props/projectile_white_horse.gd`
**extends**: `CharacterBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| priv | `_ready() -> void` | 分组、浮动模式、飞行方向、播放动画后启用物理 |
| priv | `_physics_process(delta: float) -> void` | 飞行 + move_and_collide 命中玩家 + 离屏销毁 |
| priv | `_resolve_fly_direction() -> void` | 朝玩家确定飞行方向 |
| priv | `_apply_root_facing_to_player_x() -> void` | 翻转 scale.x 对齐飞行方向 |
| priv | `_run_hit_and_despawn(body: Node2D) -> void` | 命中 -> 伤害 -> 播放 over -> 销毁 |
| priv | `_get_view_bounds_x() -> Vector2` | 获取视口水平边界 |

---

### BombRedHorse (红马炸弹)
**文件**: `system/levels/world3/props/bomb_red_horse.gd`
**extends**: `Area2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| priv | `_ready() -> void` | 禁用碰撞检测、延迟执行炸弹序列 |
| priv | `_process(delta: float) -> void` | 预警阶段缓慢追踪玩家 |
| priv | `_get_player_global_position_or_fallback(fallback: Vector2) -> Vector2` | 获取玩家位置或 fallback |
| priv | `_physics_process(_delta: float) -> void` | 爆炸阶段检测重叠玩家并造成单次伤害 |
| priv | `_run_red_bomb_sequence() -> void` | 完整序列：ready 追踪 -> boom 爆炸 -> 销毁 |
| priv | `_play_optional(anim_name: StringName, fallback_seconds: float) -> void` | 播放可选动画 |

---

### RopeToShangyang (绳子)
**文件**: `system/levels/world3/props/rope_to_shangyang.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `bind_endpoints(start_node, end_node, start_off, end_off, initial_extend) -> void` | 绑定绳索两端节点 |
| pub | `unbind_end_keep_vector_from_start() -> void` | 解绑末端但保持世界空间向量 |
| pub | `set_extend_progress(p: float) -> void` | 设置伸长进度 0~1 |
| priv | `_ready() -> void` | 初始化 Line2D 宽度和颜色 |
| priv | `_process(_delta: float) -> void` | 每帧更新 Line2D 端点 |
| priv | `_update_line_now() -> void` | 立即更新 Line2D points |

---

### BossFatalWarning
**文件**: `system/levels/world3/ui/boss_fatal_warning.gd`
**extends**: `CanvasLayer`

**信号**: `warning_finished` — 预警动画完成

| 访问 | 签名 | 描述 |
|------|------|------|
| priv | `_ready() -> void` | 播放 warning 动画或倒计时 |
| priv | `_on_animation_finished(anim_name: StringName) -> void` | warning 完成 -> 发信号 |
| priv | `_emit_done() -> void` | 发射 warning_finished + 销毁自身 |

---

### TestHorseTexturePollution
**文件**: `system/levels/world3/tests/test_horse_texture_pollution.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `_get_or_spawn_minor(minor_name: StringName) -> Node2D` | 获取或动态生成小马 |
| pub | `_play_minor_jump(minor: Node2D, minor_name: StringName) -> void` | 播放小马 jump 动画 |
| pub | `_play_minor_travel_loop_anim(minor: Node2D, minor_name: StringName) -> void` | 播放小马行走动画 |
| pub | `_set_horse_movement(node: Node, enabled: bool) -> void` | 设置小马移动状态 |
| priv | `_ready() -> void` | 创建 Camera2D、生成灰马和黑马 |
| priv | `_process(_delta: float) -> void` | 每帧监控灰马 AtlasTexture 是否被黑马污染 |

---

## 6. 可交互物系统 (Interactables)

### Interactable (交互基类)
**文件**: `system/可交互物/interactable.gd`
**class_name**: `Interactable` | **extends**: `Area2D`

**信号**: `interacted` — 交互触发时发射

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `interact() -> void` | 打印名称并发射 interacted 信号 |
| priv | `_init() -> void` | 连接 body_entered/body_exited |
| priv | `_on_body_entered(player: Player) -> void` | 注册到玩家交互列表 |
| priv | `_on_body_exited(player: Player) -> void` | 从玩家交互列表移除 |

---

### ContactTrigger (接触触发器基类)
**文件**: `system/可交互物/contact_trigger.gd`
**class_name**: `ContactTrigger` | **extends**: `Area2D`

**信号**: `interacted`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `interact() -> void` | 打印名称并发射 interacted |
| priv | `_init() -> void` | 连接 body_entered/body_exited |
| priv | `_on_body_entered(player: Player) -> void` | 注册到玩家接触触发器列表 |
| priv | `_on_body_exited(player: Player) -> void` | 从玩家接触触发器列表移除 |

---

### 触发器
**文件**: `system/可交互物/触发器.gd`
**extends**: `Area2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `enable_trigger()` | 启用 monitoring 和碰撞形状 |
| pub | `disable_trigger()` | 禁用 monitoring 和碰撞形状 |
| pub | `get_trigger_group() -> String` | 返回 trigger_group |
| pub | `set_trigger_group(new_group: String)` | 设置 trigger_group |
| pub | `set_trap_group_to_trigger(new_group: String)` | 设置并重新初始化目标陷阱组 |
| pub | `add_target_trap(trap_path: NodePath)` | 追加陷阱路径 |
| pub | `remove_target_trap(trap_path: NodePath)` | 移除陷阱路径 |
| pub | `clear_target_traps()` | 清空所有目标陷阱 |
| pub | `is_activated() -> bool` | 返回 is_triggered 状态 |
| pub | `get_target_trap_count() -> int` | 返回陷阱数量 |
| priv | `_ready()` | 连接信号、初始化目标陷阱 |
| priv | `_initialize_target_traps()` | 构建 cached_traps 列表 |
| priv | `_get_traps_by_group(group_name: String) -> Array` | 按组名查找陷阱 |
| priv | `_handle_entered(node: Node, type: String)` | 添加节点到列表，触发陷阱 |
| priv | `_handle_exited(node: Node, type: String)` | 移除节点，可能开始 reset 计时器 |
| priv | `_trigger_traps()` | 调用所有陷阱 trigger_trap() |
| priv | `_start_reset_timer()` | await reset_delay -> _reset_trigger |
| priv | `_reset_trigger()` | 重置触发状态、重新初始化陷阱 |

---

### 开关
**文件**: `system/可交互物/开关.gd`
**extends**: `StaticBody2D`

**信号**: `switch_state_changed(color: String, switch_state: String)`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `take_damage(damage_amount: float, attacker = null)` | 检查子弹触发条件后触发开关 |
| pub | `apply_switch_bus_state(target_on: bool, play_anim: bool = true) -> void` | 应用总线状态 |
| priv | `_ready()` | 初始化颜色、channel_id、视觉、总线连接 |
| priv | `_exit_tree() -> void` | 断开总线信号 |
| priv | `_deferred_sync_initial_from_bus() -> void` | 从总线同步初始状态 |
| priv | `_trigger_switch()` | 切换开关 -> 播动画 -> 发布到总线 |
| priv | `_on_channel_state_changed(ch: StringName, target_on: bool) -> void` | 匹配频道时应用总线状态 |
| priv | `_apply_switch_visual(target_on: bool) -> void` | 设置 sprite 帧 |
| priv | `_emit_state_change() -> void` | 发射 switch_state_changed 信号 |
| priv | `_play_trigger_sound() -> void` | 请求开关音效 |

---

### 窗
**文件**: `system/可交互物/窗.gd`
**extends**: `StaticBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `apply_switch_bus_state(target_open: bool, play_anim: bool = true) -> void` | 应用开关状态 + 碰撞切换 |
| priv | `_ready() -> void` | 复制碰撞形状、初始化颜色/视觉 |
| priv | `_apply_instant_visual(open: bool) -> void` | 立即设置 sprite 帧和碰撞 |
| priv | `_resolve_overlapping_players_after_open() -> void` | 推开窗口内玩家 |
| priv | `_collect_overlapping_players() -> Array[Node2D]` | 查询窗口内玩家体 |
| priv | `_push_player_out_vertical(player: Node2D, window_rect: Rect2) -> void` | 垂直推出玩家 |
| priv | `_segment_overlap(a0, a1, b0, b1) -> float` | 一维重叠计算 |
| priv | `_resolve_push_direction(player, desired_dir: float) -> float` | 推出方向计算 |
| priv | `_play_trigger_sound() -> void` | 请求开关音效 |

---

### 洞 (陷阱)
**文件**: `system/可交互物/洞.gd`
**extends**: `StaticBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `reset_state()` | 重置为未触发 |
| pub | `trigger_trap()` | 触发陷阱：播放"触发"动画 |
| pub | `enable_collision()` | 重新启用碰撞形状 |
| pub | `disable_collision()` | 禁用碰撞形状 |
| pub | `is_active() -> bool` | 碰撞是否启用 |
| pub | `get_trap_group() -> String` | 返回 trap_group |
| pub | `set_trap_group(new_group: String)` | 设置 trap_group |
| priv | `_ready()` | 配置 sprite、调用 reset_state |
| priv | `_on_animation_finished(anim_name: String)` | "触发"动画完成日志 |
| priv | `_play_trigger_sound() -> void` | 请求陷阱音效 |

---

### 水 (水滴)
**文件**: `system/可交互物/水.gd`
**extends**: `CharacterBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `initialize_from_bucket(spawn_pos: Vector2) -> void` | 设置生成位置 |
| priv | `_ready() -> void` | 碰撞层设置、动画连接、spawn + lifetime 计时器 |
| priv | `_physics_process(delta: float) -> void` | 重力 + move_and_collide + _on_hit |
| priv | `_start_spawn() -> void` | 播放生成动画 |
| priv | `_start_fall() -> void` | 播放下落动画 |
| priv | `_on_hit(collider: Object) -> void` | 造成伤害 -> disappear |
| priv | `_start_disappear() -> void` | 禁碰撞 -> disappear 动画 -> queue_free |
| priv | `_on_animation_finished(anim_name: StringName) -> void` | spawn -> fall, disappear -> queue_free |
| priv | `_start_lifetime_timer() -> void` | 超时 -> _start_disappear |
| priv | `_on_lifetime_timeout() -> void` | 存活到期 -> disappear |

---

### 桶
**文件**: `system/可交互物/桶.gd`
**extends**: `StaticBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `take_damage(damage_amount: float, attacker = null) -> void` | 检查子弹触发 -> _on_hit |
| pub | `export_save_state() -> Dictionary` | 导出桶状态（存档用） |
| pub | `apply_save_state(state: Dictionary) -> void` | 恢复桶状态（读档用） |
| priv | `_ready() -> void` | 加入 save_bucket 组、连接动画、进入 idle |
| priv | `_on_hit() -> void` | 状态机分发：开始失水循环 |
| priv | `_start_lose_water() -> void` | LOSING_WATER 状态 + 播放动画 + 生成水滴 |
| priv | `_start_add_water() -> void` | ADDING_WATER 状态 + 播放动画 |
| priv | `_enter_idle() -> void` | WITH_WATER_IDLE 状态 |
| priv | `_on_animation_finished(anim_name: StringName) -> void` | 状态转换调度 |
| priv | `_spawn_water() -> void` | 实例化水场景 |
| priv | `_resolve_water_spawn_position() -> Vector2` | 返回 WaterSpawnPoint 位置或 fallback |
| priv | `_play_trigger_sound() -> void` | 请求桶触发音效 |

---

### Teleporter
**文件**: `system/可交互物/Teleporter.gd`
**class_name**: `Teleporter` | **extends**: `Interactable`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `interact() -> void` | super.interact() + Game.change_scene(path) |

---

### AutoTeleporter
**文件**: `system/可交互物/auto_teleporter.gd`
**class_name**: `AutoTeleporter` | **extends**: `ContactTrigger`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `interact() -> void` | super.interact() + Game.change_scene(path) |

---

## 7. UI 系统

### StatusPanel
**文件**: `system/ui/status_panel.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `consume_ink_once() -> bool` | 消耗 1 格墨水 |
| priv | `_ready() -> void` | 绑定 stats、重建心格/墨格、同步显示 |
| priv | `_rebind_stats_next_frame() -> void` | 延迟重新绑定 stats |
| priv | `_process(_delta: float) -> void` | 每帧更新召唤技能 UI |
| priv | `_bind_player_stats() -> void` | 绑定玩家 stats + 连接信号 |
| priv | `_on_health_changed() -> void` | 血量变化 -> 重建心格 -> 动画队列 |
| priv | `_rebuild_hearts_if_needed(force: bool) -> void` | 按 max_health 重建心格 |
| priv | `_sync_cells_instant(health, max_health) -> void` | 立即同步所有心格状态 |
| priv | `_run_health_animation_queue() -> void` | 串行执行血量动画 |
| priv | `_apply_one_damage_step() -> void` | 执行一步扣血动画 |
| priv | `_apply_one_heal_step() -> void` | 执行一步回血动画 |
| priv | `_on_ink_changed() -> void` | 墨水变化 -> 消耗/恢复动画 |
| priv | `_calc_full_ink_slots_from_stats() -> int` | 计算满格数量 |
| priv | `_rebuild_ink_slots_if_needed(force: bool) -> void` | 按 max_ink 重建墨格 |
| priv | `_sync_ink_slots_instant(full_count: int) -> void` | 立即同步所有墨格 |
| priv | `_run_pending_ink_uses() -> void` | 串行执行墨水消耗动画 |
| priv | `_apply_one_ink_use_step() -> void` | 执行一步墨水消耗 |
| priv | `_on_ink_slot_recover_finished(slot: Node) -> void` | 墨格恢复完成 |
| priv | `_play_heart_ink_stinger(pitch: float = 1.0) -> void` | 播放心/墨音效 |
| priv | `_update_summon_skill_ui() -> void` | 更新召唤技能图标 |
| priv | `_swap_slot_with_left_neighbor(slot: Node) -> void` | 与左侧墨格交换位置 |
| priv | `_swap_slot_with_right_neighbor(slot: Node) -> void` | 与右侧墨格交换位置 |

---

### HeartCell
**文件**: `system/ui/heart_cell.gd`
**class_name**: `HeartCell` | **extends**: `Control`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `set_state_instant(next_state: int) -> void` | 立即设置状态（0 空 / 1 半 / 2 满） |
| pub | `play_damage_step() -> void` | 播放扣血一步动画（2->1 或 1->0） |
| pub | `play_heal_step() -> void` | 播放回血一步动画（0->1 或 1->2） |
| priv | `_ready() -> void` | 设置最小尺寸、精灵居中 |
| priv | `_play_if_exists(anim_name: StringName) -> void` | 播放动画（含兜底 timer） |
| priv | `_estimate_animation_seconds(frames, anim_name, fallback) -> float` | 估算动画时长 |
| priv | `_await_next_frame_safe() -> bool` | 安全等待下一帧 |

---

### InkSlot
**文件**: `system/ui/ink_slot.gd`
**class_name**: `InkSlot` | **extends**: `Control`

**信号**: `recover_finished(slot: Node)`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `set_state_instant(next_state: int) -> void` | 立即设置状态（0 空 / 1 满） |
| pub | `play_use_step() -> void` | 消耗动画：直接切 empty |
| pub | `start_recover(snapshot: Dictionary = {}) -> void` | 开始恢复动画 |
| pub | `abort_to_empty_instant() -> void` | 立即中止恢复并切空 |
| pub | `snapshot_recover_progress() -> Dictionary` | 快照当前恢复进度 |
| priv | `_ready() -> void` | 设置最小尺寸、精灵居中 |
| priv | `_play_if_exists(anim_name: StringName) -> void` | 播放动画 |
| priv | `_estimate_animation_seconds(frames, anim_name, fallback) -> float` | 估算动画时长 |
| priv | `_await_next_frame_safe() -> bool` | 安全等待下一帧 |

---

### BossHealthBar
**文件**: `system/ui/boss_health_bar.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `initialize_with_retry()` | 带重试机制的 stats 初始化（最多 10 次） |
| pub | `update_health() -> void` | Tween 更新血条值 + phase 名称动画 |
| pub | `initialize_stats() -> void` | 外部调用入口 |
| pub | `set_stats(new_stats: Stats)` | 替换 stats 节点 |
| pub | `get_current_health() -> float` | 获取当前血量 |
| pub | `get_max_health() -> float` | 获取最大血量 |
| pub | `get_health_percentage() -> float` | 获取血量百分比 |
| pub | `show_health_bar()` | 显示血条 |
| pub | `hide_health_bar()` | 隐藏血条 |
| pub | `toggle_health_bar()` | 切换可见性 |
| pub | `is_health_bar_visible() -> bool` | 检查可见性 |
| pub | `reset_health_bar()` | 重置血条 |
| pub | `get_stats_node() -> Stats` | 获取 stats 引用 |
| pub | `is_initialized_successfully() -> bool` | 检查初始化成功 |
| priv | `_ready() -> void` | 应用名称贴图/模式，延迟初始化 |
| priv | `_apply_boss_name_texture() -> void` | 应用名称贴图 |
| priv | `_apply_name_sprite_frames() -> void` | 应用动画名称卡片 |
| priv | `_apply_name_card_mode() -> void` | 切换静态/动画名称卡可见性 |
| priv | `_initialize_stats_success()` | 连接信号 + 更新血条 |
| priv | `_find_stats_node()` | 多层查找 Stats 节点 |
| priv | `_try_play_phase_name_animation(hp_percent: float) -> void` | 血量低于阈值播放 phase 动画 |
| priv | `_exit_tree()` | 断开信号连接 |

---

### SummonCooldownRing
**文件**: `system/ui/summon_cooldown_ring.gd`
**extends**: `Control`

| 访问 | 签名 | 描述 |
|------|------|------|
| priv | `_ready() -> void` | 设置最小尺寸 |
| priv | `_draw() -> void` | 绘制背景弧 + 按 cooldown_ratio 填充前景弧 |

---

### World3DeathRetryUI
**文件**: `system/ui/world3_death_retry_ui.gd`
**extends**: `CanvasLayer`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `bind_game(game_node: Node) -> void` | 绑定 Game 节点并刷新按钮状态 |
| priv | `_play_ui_click() -> void` | 播放 UI 点击音效 |
| priv | `_ready() -> void` | 连接按钮信号、刷新状态、显示"死"图文 |
| priv | `_refresh_heart_button_state() -> void` | 根据存档启用/禁用"读档心"按钮 |
| priv | `_on_press_heart() -> void` | "读档心" -> on_world3_death_choice_load_heart + queue_free |
| priv | `_on_press_savepoint() -> void` | "读档存档" -> on_world3_death_choice_load_savepoint + queue_free |

---

## 8. 镜头系统 (Camera)

### CameraCueFocusArea
**文件**: `system/camera/camera_cue_focus_area.gd`
**class_name**: `CameraCueFocusArea` | **extends**: `Area2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `export_save_state() -> Dictionary` | 导出存档状态 |
| pub | `apply_save_state(state: Dictionary) -> void` | 恢复存档状态 |
| priv | `_ready() -> void` | 加入 save_camera_cue 组、连接信号、刷新碰撞形状 |
| priv | `_refresh_collision_shape() -> void` | 动态更新 CollisionShape2D |
| priv | `_process(_delta: float) -> void` | 每帧计算插值目标驱动相机 |
| priv | `_on_body_entered(body: Node2D) -> void` | 玩家进入 -> 开始追踪焦点 |
| priv | `_on_body_exited(body: Node2D) -> void` | 玩家离开 -> 回正相机 |

---

### CameraFocusArea
**文件**: `system/camera/camera_focus_area.gd`
**class_name**: `CameraFocusArea` | **extends**: `CameraCueFocusArea`

> 别名类，无新增函数。全部继承自 CameraCueFocusArea。

---

### CameraCuePanToMarker
**文件**: `system/camera/camera_cue_pan_to_marker.gd`
**class_name**: `CameraCuePanToMarker` | **extends**: `Area2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `export_save_state() -> Dictionary` | 导出存档状态 |
| pub | `apply_save_state(state: Dictionary) -> void` | 恢复存档状态 |
| priv | `_ready() -> void` | 加入 save_camera_cue 组、连接信号 |
| priv | `_resolve_target_marker() -> Marker2D` | 解析目标 Marker |
| priv | `_on_body_entered(body: Node2D) -> void` | 玩家进入 -> 启动平移序列 |
| priv | `_on_body_exited(body: Node2D) -> void` | 玩家离开（不中断进行中的平移） |
| priv | `_run_pan_sequence() -> void` | 核心序列：禁用输入 -> 平移 -> 停留 -> 回正 -> 恢复输入 |
| priv | `_resolve_player_camera2d(player: Node2D) -> Camera2D` | 获取玩家 Camera2D |
| priv | `_estimate_travel_seconds(cam: Camera2D, target: Vector2, speed: float) -> float` | 估算移动耗时 |
| priv | `_resolve_player_camera_aim_world(player: Node2D) -> Vector2` | 获取 CameraAimMarker 世界坐标 |
| priv | `_clamp_world_to_camera_limits(cam: Camera2D, world: Vector2) -> Vector2` | 钳制到 Camera2D limit 范围 |
| priv | `_wait_seconds_safe(seconds: float) -> void` | 安全 await 等待 |

---

## 9. 机关 / 对话 / 场景 / 工具

### MechanismChannelIds
**文件**: `system/mechanisms/mechanism_channel_ids.gd`
**class_name**: `MechanismChannelIds` | **extends**: `RefCounted`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub_static | `color_to_channel_id(color_name: String) -> StringName` | 中文颜色名映射为数字序号通道 ID |

---

### SwitchChannelListener
**文件**: `system/mechanisms/switch_channel_listener.gd`
**extends**: `Node`

| 访问 | 签名 | 描述 |
|------|------|------|
| priv | `_ready() -> void` | 解析通道 ID、连接总线、延迟同步初始状态 |
| priv | `_exit_tree() -> void` | 断开总线信号连接 |
| priv | `_resolve_channel_id() -> StringName` | 返回 channel_id 或从父节点 initial_color 映射 |
| priv | `_deferred_sync_initial_from_bus() -> void` | 延迟从总线同步初始状态 |
| priv | `_on_channel_state_changed(ch: StringName, is_open: bool) -> void` | 匹配通道时调用父节点 apply_switch_bus_state |
| priv | `_apply_to_parent(is_open: bool, play_anim: bool) -> void` | 向父节点调用 apply_switch_bus_state |

---

### 到区域触发对话
**文件**: `system/dialogic/到区域触发对话.gd`
**extends**: `Area2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `reset_trigger()` | 重置触发器状态 |
| priv | `_ready()` | 连接 body_entered/exited |
| priv | `_process(delta)` | 冷却计时 + 交互键触发 |
| priv | `_on_body_entered(body: Node2D)` | 玩家进入 -> 自动触发 |
| priv | `_on_body_exited(body: Node2D)` | 玩家离开 -> 清除状态 |
| priv | `_is_player(body: Node) -> bool` | 判断是否为 CharacterBody2D |
| priv | `_trigger_dialogue()` | 检查条件后调用 _start_dialogic_dialogue |
| priv | `_start_dialogic_dialogue()` | 加载时间线 -> Dialogic.start -> 连接结束信号 |
| priv | `_on_dialog_ended()` | 对话结束回调 |

---

### TitleScreen
**文件**: `system/scene/title_screen.gd`
**extends**: `Control`

| 访问 | 签名 | 描述 |
|------|------|------|
| priv | `_ready() -> void` | "新游戏"按钮获取焦点 |
| priv | `_on_new_game_pressed() -> void` | Game.new_game() |
| priv | `_on_load_game_pressed() -> void` | Game.load_game() |
| priv | `_on_exit_game_pressed() -> void` | get_tree().quit() |

---

### Loading
**文件**: `system/scene/loading.gd`
**extends**: `Node2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `setup_display_mode()` | 设置 VSync、全屏、最大帧率 60 |
| pub | `play_loading_animation()` | 播放 AnimationPlayer "new_animation" |
| priv | `_ready()` | 设置显示模式 -> 等待一帧 -> 播放加载动画 |

---

### Stats
**文件**: `system/class/stats.gd`
**class_name**: `Stats` | **extends**: `Node`

**信号**: `health_changed` / `ink_changed`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `to_dict() -> Dictionary` | 导出 max_health, health, max_ink, ink |
| pub | `from_dict(dict: Dictionary) -> void` | 从字典恢复属性值 |

> `health` 和 `ink` 为带 setter 的 `@onready var`，自动钳制和发射信号。

---

### Tip开
**文件**: `system/tips/tip_开.gd`
**extends**: `Area2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `reset_trigger()` | 重置触发状态 |
| pub | `trigger_dialogue_manually()` | 外部手动触发对话 |
| pub | `set_dialogue_timeline(new_timeline: String)` | 运行时切换对话时间线 |
| priv | `_ready()` | 连接 body_entered/exited、隐藏指示器 |
| priv | `_process(delta)` | 检测交互键触发对话 |
| priv | `_on_body_entered(body: Node2D)` | 显示指示器 |
| priv | `_on_body_exited(body: Node2D)` | 隐藏指示器 |
| priv | `_trigger_dialogue()` | one_shot 检查后启动对话 |
| priv | `_start_dialogic_dialogue()` | 调用 Dialogic2 启动时间线 |

---

### 门 (根目录)
**文件**: `门.gd` (项目根)
**extends**: `StaticBody2D`

| 访问 | 签名 | 描述 |
|------|------|------|
| pub | `apply_switch_bus_state(target_open: bool, play_anim: bool = true) -> void` | 同步门的开关状态 |
| priv | `_ready()` | 初始化颜色和门状态（开/关 sprite 帧 + 碰撞） |
| priv | `push_players_out()` | 推出撞到关门上的玩家 |
| priv | `_play_trigger_sound() -> void` | 请求开关触发音效 |

---

### DefaultLayoutBase (Dialogic 自定义样式)
**文件**: `addons/dialogic/样式/CustomMyStyle/default_layout_base.gd`
**@tool** | **extends**: `DialogicLayoutBase`

| 访问 | 签名 | 描述 |
|------|------|------|
| priv | `_apply_export_overrides() -> void` | 将导出变量应用到 DialogicLayoutBase 属性 |

---

## 附录：关键枚举定义

### BossHorseTypes.HorseId
| 值 | 含义 |
|----|------|
| 0 = GREY | 灰马 |
| 1 = WHITE | 白马 |
| 2 = BLACK | 黑马 |
| 3 = RED | 红马 |

### BossHorseTypes.BossPhase
| 枚举值 | 含义 |
|--------|------|
| INTRO | 入场介绍 |
| SI_TO_FOUR_VISUAL | 驷马分裂视觉效果 |
| FOUR_EXIT_GREY_ENTER | 驷马离场 + 灰马入场 |
| GREY_SOLO | 灰马单挑 |
| WHITE_SOLO | 白马单挑 |
| BLACK_SOLO | 黑马单挑 |
| RED_SOLO | 红马单挑 |
| FINAL_WARNING_20 | 20% 致命预警 |
| CHAIN_CINEMATIC | 五马分尸演出 |
| ALL_HORSES | 全部马匹围杀 |

### Player.PlayerState (推断)
IDLE, WALK, JUMP_ASCEND, JUMP_FALL, ATTACK_START, ATTACK_SHOOT, ATTACK_END, STUNNED, SUMMON_START, SUMMON_END

### ShangYangLimbPiece.LimbType
HEAD, HAND, FOOT

### 桶.BucketState (推断)
WITH_WATER_IDLE, LOSING_WATER, ADDING_WATER