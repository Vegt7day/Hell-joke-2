# 机关音频配置检查清单

本清单用于排查当前工程中的机关音频配置缺口。

## 检查范围

- 机关场景：`system/可交互物/*.tscn` 与对应 `*.gd`
- 额外补充：全项目（不含 `addons`）中“有音频节点/引用但未绑定有效声音”的项

---

## A. 机关场景中未配置任何音频节点的项

以下场景已具备交互/动画逻辑，但场景内未发现 `AudioStreamPlayer*` 节点：

- `system/可交互物/触发器.tscn`
- `system/可交互物/hell.tscn`
- `system/可交互物/contact_trigger.tscn`
- `system/可交互物/interactable.tscn`
- `system/可交互物/auto_teleporter.tscn`

说明：

- 已补充触发音效的场景：`开关/门/窗/桶/水/洞`。

---

## B. 有音频节点/引用但未绑定真实声音的项（全项目补充）

以下为“节点存在但无 `stream`”或“脚本引用音频节点但场景缺少该节点”：

1. `entities/enemy/bullet/蚊子.tscn`
   - 节点：`disappear`（`AudioStreamPlayer`）
   - 问题：未设置 `stream`

2. `system/levels/world.tscn`
   - 节点：`AudioStreamPlayer`（根节点下）
   - 问题：未设置 `stream`

---

## C. 已配置音频的对照项（便于参考）

- `entities/player/bullet/ink.tscn`：`disapear` 已绑定 `stream`
- `entities/enemy/teacher/teacher.tscn`：`shoot` 已绑定 `stream`
- `entities/enemy/bullet/蚊子.tscn`：`shooting` 已绑定 `stream`
- `entities/player/real player/student.tscn`：`walk`、`jump`、`attack` 已绑定 `stream`

---

## D. 建议优先级

1. **高优先级**
   - 修复 `student.gd` 的 `summon_sound` 悬空节点引用（避免运行时空引用风险）
2. **中优先级**
   - 为其余可交互物补齐触发动作音效（`触发器/hell/contact_trigger/interactable/auto_teleporter`）
3. **低优先级**
   - 清理 `world.tscn` 未配置 `stream` 的孤立 `AudioStreamPlayer`（确认是否预留）

