# 修改记录

## 2026-05-03

### 新增

| 文件 | 类型 | 描述 |
|------|------|------|
| `system/ui/mini_map.gd` | 脚本 | 右上角缩略地图：21×21 格，支持机关/墙壁/地板渲染，菜单背景，HUD 光标 |
| `system/ui/mini_map.tscn` | 场景 | 缩略地图独立场景，NinePatchRect 菜单背景 + MiniMap MapDraw 子节点 |

### 修改

| 文件 | 改动 |
|------|------|
| `system/globals/cursor_manager.gd` | 全局悬浮检测 + 全模式点击反馈；_apply_hover_cursor 移除递归调用；set_cursor_type 简化；_type_before_hover 支持 INSPECT |
| `system/globals/game/game.gd` | _on_player_stats_changed 不再限制世界3；_open_world3_death_retry_ui 调用 stop_health_animation |
| `entities/player/real player/viewport_nav_hud.gd` | X 键切换问号模式；_enter/exit_inspect_mode 暂停/恢复游戏；弹出钳制+父节点边界；intersect_point 搜索半径回退 |
| `entities/player/real player/student.tscn` | 增加 MiniMap 实例；修复 heart_cell_scene/ink_slot_scene null 覆盖 |
| `system/ui/status_panel.gd` | 新增 _show_hotbar_tooltip 快捷栏切换提示（0.5s 渐隐）；stop_health_animation()；快捷栏格子 StyleBoxTexture； 加入 status_panel 组 |
| `system/ui/heart_cell.gd` | 新增 _speed_mult / set_animation_speed()，支持动画加速 |
| `system/ui/inventory/player_inventory.gd` | add_item 改为无上限堆叠，同 ID 物品全部叠加 |
| `system/ui/inventory/inventory_ui.gd` | 修复快捷栏拖拽同时拿起下方格子物品（gui_input 事件消费 + hotbar 区域检测） |
| `system/ui/abstract_map/abstract_map_ui.gd` | 添加菜单背景 + ColorRect 区域限制；退出保存 _map_origin/_zoom_mul；快捷键退出改为 _close_map() |
| `system/ui/abstract_map/abstract_map_draw.gd` | 新增 static _saved_map_origin/_saved_zoom_mul 跨实例持久化地图状态 |
| `system/ui/inspect_detector.gd` | 世界坐标→视口坐标转换；弹窗定位钳制到父节点；清理冗余 print |
| `system/ui/save_slots_sheet.gd` | var id 类型显式标注 String |
| `system/ui/world3_death_retry_ui.gd` | @onready 路径添加 MenuBack；按钮改为打开读档界面；"回到上一个心" 可见性改为无存档时隐藏 |

### 修复

- 问号模式下离开按钮不恢复问号光标（_apply_hover_cursor 递归覆盖 _type_before_hover）
- 退出问号后按钮无法点击（_exit_inspect_mode 未恢复 PROCESS_MODE_INHERIT）
- 快捷栏拖拽时下方背包格子物品消失（_drag_source_is_hotbar 干扰 grid 刷新）
- world2 死亡不触发（去掉 _is_world3_boss_scene 检查）
- world3 死亡后心脏动画持续播放（game.gd 调用 stop_health_animation）
- 背包物品自动堆叠未生效（student.tscn heart_cell_scene = null 覆盖 preload）
- 问号模式死亡 UI 按钮失灵（@onready 路径错误）
- 地图打开报错（@onready 路径未更新）